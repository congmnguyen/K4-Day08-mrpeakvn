# Dependencies để CHẠY app + test + pipeline Task 1-10.
# Hugging Face Spaces cài đúng file này, nên chỉ giữ những gì thật sự được import.
# Dependencies chỉ dùng cho evaluation nằm ở requirements-eval.txt.

# Core
python-dotenv>=1.0.0
requests>=2.31.0
certifi>=2024.2.2
numpy>=1.24.0
beautifulsoup4>=4.12.0

# Task 2 - Crawling
# Không dùng crawl4ai: nó kéo theo playwright + bộ browser rất nặng, trong khi
# src/task2_crawl_news.py chỉ cần requests + BeautifulSoup.

# Task 3 - Document conversion
markitdown[pdf]>=0.1.0
fpdf2>=2.7.0
# Task 3 còn cần LibreOffice (`soffice`) ở tầng hệ điều hành để đọc .doc Công báo.

# Task 4 - Chunking & Indexing
langchain-text-splitters>=0.2.0
chromadb>=0.5.0

# Task 4/5/10 - Embedding + Generation (OpenAI)
openai>=1.30.0

# Task 6 - BM25
rank-bm25>=0.2.2

# Task 8 - PageIndex (nhánh cloud, tùy chọn)
pageindex>=0.1.0

# Group project - UI
streamlit>=1.35.0
# ui/components.py import trực tiếp markdown_it; hiện nó chỉ vào máy nhờ rich
# (dependency của streamlit) nên phải khai báo tường minh.
markdown-it-py>=3.0.0

# Testing (dung de cham diem Task 1-10)
pytest>=7.0.0
